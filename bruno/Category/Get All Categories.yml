info:
  name: Get All Categories
  type: http
  seq: 9

http:
  method: GET
  url: localhost:8080/api/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
