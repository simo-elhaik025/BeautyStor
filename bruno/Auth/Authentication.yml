info:
  name: Authentication
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "elhaik025@gmail.com",
        "password": "87651234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
