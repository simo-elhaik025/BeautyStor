info:
  name: Get User By ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/users/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
