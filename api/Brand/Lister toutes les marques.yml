info:
  name: Lister toutes les marques
  type: http
  seq: 2

http:
  method: GET
  url: " localhost:8080/api/brands"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
