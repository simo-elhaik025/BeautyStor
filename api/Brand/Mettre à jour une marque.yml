info:
  name: Mettre à jour une marque
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/api/brands/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
