info:
  name: Consultation d'une commande
  type: http
  seq: 2

http:
  method: GET
  url: localhost:8080/api/orders/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
