info:
  name: Créer une image produit
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/product-images
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
