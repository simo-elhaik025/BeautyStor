info:
  name: Mettre à jour une image
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/api/product-images/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
