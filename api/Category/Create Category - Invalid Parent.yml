info:
  name: Create Category - Invalid Parent
  type: http
  seq: 4

http:
  method: POST
  url: localhost:8080/api/categories
  body:
    type: json
    data: |-
      {
        "name": "Makeup",
        "slug": "makeup",
        "parentId": -1,
        "active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
