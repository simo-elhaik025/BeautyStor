info:
  name: Create Category - Success
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/categories
  body:
    type: json
    data: |-
      {
        "name": "Meckup-2",
        "slug": "meckup-2",
        "parentId": null,
        "active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
