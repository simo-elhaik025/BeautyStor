info:
  name: Create Category - Missing Name
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/api/categories
  body:
    type: json
    data: |-
      {
        "slug": "makeup",
        "parentId": null,
        "active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
