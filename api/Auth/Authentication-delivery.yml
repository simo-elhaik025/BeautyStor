info:
  name: Authentication-delivery
  type: http
  seq: 5

http:
  method: POST
  url: localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "youssef.test@beautystor.com",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
